# Evidence

This directory is empty by design. The current P104 witness is derived from
the literal reference dynamics, so it needs no opaque stored table. A promoted
result that does require finite data belongs here with its immutable data, a
checker, the precise statement it supports, and a record of the checker’s
trusted assumptions. Search traces stay in [`research/`](../research/) until
they meet that standard.

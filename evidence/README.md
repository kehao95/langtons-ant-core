# Evidence

This directory is empty by design. A promoted finite result belongs here only
with its immutable data, a checker, the precise statement it supports, and a
record of the checker’s trusted assumptions. Search traces stay in
[`research/`](../research/) until they meet that standard.
